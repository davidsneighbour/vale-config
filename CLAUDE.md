# CLAUDE.md

Before doing any work in this repository, read `AGENTS.md` and execute the instructions in that file. Treat `AGENTS.md` as the shared source of truth for repository workflow, architecture, commands, and conventions.

This file only provides Claude Code-specific guidance for `claude.ai/code`.

## Claude Code notes

- Keep shared agent instructions in `AGENTS.md`.
- Add Claude-only guidance here only when it applies specifically to Claude Code and does not apply to other agents.
